<?xml version="1.0" encoding="utf-8"?>
<android.support.v4.widget.DrawerLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/regions_layout_drawer"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:fitsSystemWindows="true"
    android:layout_gravity="end"
    tools:openDrawer="end">

    <android.support.v7.widget.RecyclerView
        android:id="@+id/regions_layout_recycler_view"
        android:layout_width="match_parent"
        android:layout_height="wrap_content" />

    <include
        android:id="@+id/regions_layout_button_menu"
        layout="@layout/button_menu" />

    <include
        android:id="@+id/regions_layout_button_map"
        layout="@layout/button_map" />

    <android.support.design.widget.NavigationView
        android:id="@+id/regions_layout_navigation_view"
        android:layout_width="wrap_content"
        android:layout_height="match_parent"
        android:layout_gravity="end"
        android:fitsSystemWindows="true"
        android:background="@color/background"
        app:headerLayout="@layout/navigator_header_layout"
        app:menu="@menu/navigator_header_menu" />

</android.support.v4.widget.DrawerLayout>