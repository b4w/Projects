<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.triangularlake.constantine.triangularlake">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme">

        <activity
            android:name=".activities.RegionActivity"
            android:label="Triangular Lake">
            <intent-filter>
                <action android:name="ANDROID.INTENT.ACTION.MAIN" />
                <category android:name="ANDROID.INTENT.CATEGORY.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".activities.LietlahtiActivity">
            <intent-filter>
                <action android:name="LietlahtiActivity" />
                <category android:name="ANDROID.INTENT.CATEGORY.DEFAULT" />
            </intent-filter>
        </activity>

    </application>

</manifest>
